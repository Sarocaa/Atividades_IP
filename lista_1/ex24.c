#include <stdio.h>
int main()
{
    int num1, num2, num3, mmc, multiplicador, divisor;
    scanf("%d %d %d", &num1, &num2, &num3);
    divisor = 2;
    multiplicador = 1;
    mmc = 0;

    if (num1!=0 && num1>=1)
    {
        if (num2!=0 && num2>=1)
        {
            if (num3!=0 && num3>=1)
            {
                printf("%d %d %d", num1, num2, num3)
                for (divisor=2; divisor++)
                {
                    num1 = num1/divisor;
                    num2 = 
                }
            }
        }
    }
    return 0;
}